{
  "name": "server",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node --trace-warnings -r dotenv/config ./index.js",
    "start:prod": "node -r dotenv/config dist",
    "clean": "rm -r dist",
    "build": "npm ./build.js",
    "build:prod": "cross-env NODE_ENV=production npm ./build.js"
  },
  "author": "Jacob Lindahl <jacob@near.foundation>",
  "license": "GPL-3.0-only",
  "dependencies": {
    "@koa/cors": "^3.1.0",
    "@koa/router": "^10.0.0",
    "canvas": "^2.8.0",
    "dotenv": "^10.0.0",
    "koa": "^2.13.1",
    "node-cron": "^3.0.0",
    "slonik": "^23.8.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "db-migrate": "^1.0.0-beta.16",
    "db-migrate-pg": "^1.2.2",
    "esbuild": "^0.13.8",
    "typescript": "^4.4.4"
  },
  "engines": {
    "node": "14.x",
    "npm": ">=7"
  }
}
